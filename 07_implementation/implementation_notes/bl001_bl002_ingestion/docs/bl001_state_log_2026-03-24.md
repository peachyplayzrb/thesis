# BL-001 State Log - Spotify Ingestion Contract

**Log Date:** 2026-03-25
**Stage:** BL-001 (Ingestion Schema Contract)
**Scope Type:** Contract/specification stage (non-runtime)
**Primary Evidence Files:**
- 06_data_and_sources/schema_notes.md
- 07_implementation/implementation_notes/bl001_bl002_ingestion/ingest_history_parser.py
- 07_implementation/implementation_notes/bl001_bl002_ingestion/docs/spotify_schema_reference.md
- 07_implementation/implementation_notes/bl001_bl002_ingestion/spotify_mapping.py
- 07_implementation/backlog.md

---

## 1. Purpose
BL-001 defines the normalized ingestion contract used by BL-002 and downstream stages. It specifies required fields, optional fields, validation policy, and row-quality semantics for Spotify-derived listening evidence.

## 2. Input/Output Contract
- Input shape: Spotify listening-history style records from CSV and API-derived sources routed into common normalized fields.
- Required logical fields: `track_name`, `artist_name`, `played_at`, `ms_played`.
- Optional/enrichment fields: `album_name`, `platform`, `isrc`.
- Normalized contract fields include:
  - `event_id`
  - `track_name`
  - `artist_name`
  - `album_name`
  - `isrc`
  - `played_at`
  - `ms_played`
  - `source_platform`
  - `ingest_run_id`
  - `row_quality_flag`

## 3. Processing Policy (Defined)
- Validation policy distinguishes hard-invalid rows from soft-missing rows.
- `missing_isrc` is soft-missing (row remains valid for downstream use).
- Hard-invalid categories include missing core field, invalid timestamp, and invalid `ms_played`.

## 4. Current State Snapshot
- BL-001 backlog status remains `done`.
- BL-001 contract semantics remain active for ingestion-quality definitions and normalized field intent.
- Active production acquisition is API-first via BL-002 export artifacts.
- `ingest_history_parser.py` remains the CSV-normalization reference implementation and validation utility, but is not the default path in BL-013 orchestrated runs.

## 5. Contract Evidence
- Schema definition and policy: `06_data_and_sources/schema_notes.md`
- Normalization implementation and quality classification: `07_implementation/implementation_notes/bl001_bl002_ingestion/ingest_history_parser.py`
- Current exported schema reference: `07_implementation/implementation_notes/bl001_bl002_ingestion/docs/spotify_schema_reference.md`
- Field-level mapping definitions: `07_implementation/implementation_notes/bl001_bl002_ingestion/spotify_mapping.py`
- Completion/traceability: `07_implementation/backlog.md`

## 6. Dependency and Alignment Check
- BL-002 export outputs align with the documented ingestion schema and mapping module fields.
- BL-003 consumes BL-002 flat outputs (`top_tracks`, `saved_tracks`, `playlist_items`, `recently_played`) and preserves BL-001 contract intent for downstream seed construction.
- No contract-breaking field drift observed in current ingestion artifacts.

## 7. Runtime Path Clarification
- Canonical ingestion runtime path: `export_spotify_max_dataset.py` -> `outputs/spotify_api_export/*` -> BL-003 DS-001 seed build.
- BL-001 parser contract path remains available for CSV-style ingestion normalization and policy checks.
- This split is intentional: BL-001 governs contract semantics; BL-002 owns active API acquisition.

## 8. Constraints / Risks
- Main risk remains documentation drift between contract wording and API-first runtime usage.
- Contract itself is stable; operational risk is concentrated in BL-002 external API/auth dependencies.

## 9. Recommended Follow-up
1. Keep BL-001 marked `done`.
2. Keep `schema_notes.md` wording explicitly acquisition-agnostic.
3. Keep `spotify_schema_reference.md` synchronized with any future field additions in `spotify_mapping.py`.

## 10. Conclusion
BL-001 is complete and currently aligned with implemented ingestion behavior.
